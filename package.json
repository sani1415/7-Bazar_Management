{
  "name": "bazar-management-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Grocery Management System",
  "scripts": {
    "create-bucket": "node scripts/create-memo-bucket.js",
    "build": "echo App folder is the web app",
    "icons:android": "node scripts/generate-android-icons.js",
    "splash:android": "node scripts/generate-android-splash.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@supabase/supabase-js": "^2.95.3",
    "sharp": "^0.34.5"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "hijri-converter": "^1.1.1"
  }
}
